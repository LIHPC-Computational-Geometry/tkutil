name: Spack CI

on:
  push:
    branches:
      - '**'
    tags:
      - '*'
  pull_request:
  # Allows you to run this workflow manually
  workflow_dispatch:

jobs:
  call:
    name: Call Spack reusable workflow
    uses: LIHPC-Computational-Geometry/tkutil/.github/workflows/spack-reusable.yml@newci
